% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simpsons_paradox.R
\name{create_microclusters_sparse}
\alias{create_microclusters_sparse}
\title{Create microclusters from a sparse expression matrix}
\usage{
create_microclusters_sparse(
  expr_mat,
  n_clusters = NULL,
  n_pcs = 20,
  min_cells = 30,
  use_binary_pca = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{expr_mat}{Sparse matrix (genes x cells), raw counts.}

\item{n_clusters}{Target number of microclusters, or \code{NULL} (default)
to use the natural community structure found by Leiden at resolution 1.
When an integer is given, the resolution achieving roughly that many
clusters is found by bisection (see \code{leiden_at_target}) and the
realised count is returned in \code{n_clusters} of the result. \code{NULL}
performs substantially better on the benchmarks -- see the vignette.}

\item{n_pcs}{Number of principal components. Default \code{20}.}

\item{min_cells}{Minimum cells per microcluster; smaller clusters
are merged. Default \code{30}.}

\item{use_binary_pca}{Logical; build the embedding from the binarised
detection matrix instead of normalized expression. Default \code{FALSE}.}

\item{verbose}{Logical; print progress? Default \code{TRUE}.}
}
\value{
A list with components:
\describe{
\item{assignments}{Named integer vector of cluster assignments.}
\item{sizes}{Table of cluster sizes.}
\item{n_clusters}{Final number of clusters.}
}
}
\description{
Performs PCA on log-normalized expression, builds an SNN graph on the
PCA embedding and clusters cells into microclusters with Leiden. Small
clusters are merged into their nearest larger neighbor.
}
\examples{
sce <- sce_example()
mat <- SingleCellExperiment::counts(sce)
clusters <- create_microclusters_sparse(mat, min_cells = 20)
clusters$n_clusters
table(clusters$assignments)

}
